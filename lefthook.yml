pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,vue,js,mjs,cjs}"
      run: npx eslint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{ts,vue,js,mjs,cjs,json,css,md}"
      run: npx prettier --write {staged_files}
      stage_fixed: true
